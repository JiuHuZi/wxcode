<view class="body">
  <view class="tab">
    <view wx:for="{{list}}" wx:key='this' bindtap="click" data-id="{{index}}" class="sheet {{item.active?'active':''}}">{{item.name}}</view>
  </view>
  <view>
    <image src="{{src}}" style="width:100%;height:493px;margin: 0 auto;"/>
  </view>
  <view class="footer">
    <text>尤克里里 yeah!</text>
  </view>
</view>