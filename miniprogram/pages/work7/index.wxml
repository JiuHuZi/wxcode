<view class="content">
    <view class="search">
        <i class="icon-weizhi iconfont weizhi"></i>
        <!-- <text>城市：</text> -->
        <input type="text" placeholder="城市" bindinput="input" class="search_ipt" placeholder-style="color:#fff;" />
        <i class="icon-sousuo1 iconfont weizhi" bindtap="search"></i>
    </view>
    <view class="today">
        <view class="city">{{city}}天气预报</view>
        <view class="temp">{{temperature}}°</view>
        <view class="wStatus">{{wStatus}}</view>
        <view class="msg">
            <view>湿度：{{humidity}}%</view>
            <view>风向：{{direct}}</view>
        </view>
        <view class="msg">
            <view>风力：{{wPower}}级</view>
            <view>气压：{{kPa}}</view>
        </view>
        <!-- <view>经度：{{longitude}}</view>
        <view>纬度：{{latitude}}</view> -->
    </view>

    <!-- 未来三天 -->
    <view class="foot" wx:if="{{isfoot}}" style="isfoot?'display:block;':'display:none;'">
        <view class="feture">
            <view wx:for="{{weather}}" wx:key='this'>
                <view>{{item.day}}</view>
                <view>{{item.wType}}</view>
                <view>{{item.maxTemp}}/{{item.minTemp}}°C</view>                
            </view>
        </view>
    </view>


</view>